{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "This is a chat app",
  "main": "server",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "author": "Rexter",
  "license": "ISC",
  "dependencies": {
    "socket.io": "^3.1.1"
  }
}
